<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "../mybatis-3-config.dtd" >

<configuration>
	<!-- 读取小配置文件信息 -->
	<properties resource="jdbc.properties"/>
	
	<!-- 定义别名 -->
	<typeAliases>
		<typeAlias alias="User" type="com.zufangwang.entity.User"/>
		<typeAlias alias="Category" type="com.zufangwang.entity.Category"/>
		<typeAlias alias="House" type="com.zufangwang.entity.House"/>
		<typeAlias alias="order" type="com.zufangwang.entity.Order"/>
	</typeAliases>
	
	<environments  default="oracle">
		<environment id="oracle">
			<transactionManager type="JDBC"></transactionManager>
			<dataSource type="POOLED">
				<property name="driver" value="${driver}"></property>
				<property name="url" value="${url}"></property>
				<property name="username" value="${username}"></property>
				<property name="password" value="${password}"></property>
			</dataSource>
		</environment>
	</environments>	
	<!-- mapper文件的注册 -->
	<mappers>
		<mapper resource="UserMapper.xml"></mapper>
		<mapper resource="CategoryMapper.xml"></mapper>
		<mapper resource="HouseMapper.xml"></mapper>
		<mapper resource="OrderMapper.xml"></mapper>
	</mappers>
</configuration>

